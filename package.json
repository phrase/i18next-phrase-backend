{
  "name": "i18next-phrase-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "browser": "browserify --ignore cross-fetch --standalone i18nextPhraseBackend dist/i18nextPhraseBackend.js -o dist/out.js",
    "build": "npm run compile && npm run browser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
